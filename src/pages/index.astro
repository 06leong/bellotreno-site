---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="BelloTreno" pageScripts={["/scripts/station.js", "/scripts/main.js"]}>
    <Fragment slot="head"></Fragment>
    <div class="container">
        <div class="text-center mt-10 mb-6">
            <a href="#" class="inline-block hover:opacity-80 transition-opacity" onclick="goHome(); return false;">
                <h1 class="text-[2.5rem] font-bold mb-2 text-primary">BelloTreno</h1>
            </a>

            <div id="statsBar" class="grid grid-cols-[1fr_auto_1fr] items-center gap-3 sm:gap-4 mb-4 text-xs sm:text-sm text-base-content/60 transition-opacity duration-500 opacity-0">
                <div class="flex items-center gap-1.5 justify-self-end">
                    <span class="relative flex h-2 w-2">
                        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-success opacity-75"></span>
                        <span class="relative inline-flex rounded-full h-2 w-2 bg-success"></span>
                    </span>
                    <span id="statsCircolanti">--</span>
                    <span data-i18n="stats_running">趟列车正在运行</span>
                </div>
                <span class="text-base-content/20">|</span>
                <div class="flex items-center gap-1.5 justify-self-start">
                    <span class="inline-flex rounded-full h-2 w-2 bg-base-content/30"></span>
                    <span data-i18n="stats_today_prefix">今日已运行</span>
                    <span id="statsGiorno">--</span>
                    <span data-i18n="stats_today_suffix">趟列车</span>
                </div>
            </div>

            <div class="relative w-full max-w-2xl mx-auto group">
                <button id="searchModeToggle" onclick="switchSearchMode(searchMode === 'train' ? 'station' : 'train')" class="absolute left-2 top-1/2 -translate-y-1/2 btn btn-ghost btn-sm rounded-full z-10 gap-0.5 px-2.5 text-base-content/50 hover:text-primary transition-colors">
                    <span class="material-symbols-outlined text-[1.3rem]" id="searchIcon">train</span>
                    <span class="material-symbols-outlined text-[0.9rem]">expand_more</span>
                </button>
                <input type="text" id="trainSearch" class="input shadow-sm w-full pl-20 py-6 text-lg bg-base-100 border-base-300 focus:outline-none focus:border-primary focus:ring-4 focus:ring-primary/20 transition-all text-base-content placeholder-base-content/40 rounded-full" placeholder="" autocomplete="off" />
            </div>

            <div id="recentSearchesContainer" class="recent-searches-container" style="display: none;">
                <div class="recent-searches-label">
                    <span class="material-symbols-outlined">history</span>
                    <span data-i18n="recent_searches">最近搜索</span>
                </div>
                <div id="recentSearchesChips" class="recent-searches-chips"></div>
            </div>
        </div>

        <div id="disambiguation">
            <h3 style="margin-top:0;" class="text-primary" data-i18n="disambiguation_title">检测到多个班次，请选择：</h3>
            <div id="choicesList"></div>
        </div>

        <div id="scTooltip" class="sc-tooltip"></div>
        <div id="results">
            <div id="trainCard" class="train-header"></div>
            <div id="smartCaringCard" class="sc-card" style="display:none;"></div>
            <div class="timeline-container">
                <div id="timelineBody"></div>
            </div>
        </div>
    </div>
</BaseLayout>
