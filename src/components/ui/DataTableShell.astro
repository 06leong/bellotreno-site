---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className = '', ...attrs } = Astro.props;
---

<div {...attrs} data-slot="table-container" class:list={['bt-data-table-shell', 'bg-card text-card-foreground', className]}>
  <slot />
</div>
